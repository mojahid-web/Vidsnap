name: Keep Alive
on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping GrabSnap Backend
        run: |
          curl -s https://vidsnap-5ilq.onrender.com/
          echo "Backend pinged at $(date)"
